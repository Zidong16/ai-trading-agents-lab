# Template — copy to `.streamlit/secrets.toml` and fill in.
# The real secrets.toml is gitignored so your password never lands in git.

# Fixed access password for the login gate.
app_password = "change-me"

# Path to your local TradingAgents checkout (the folder with pyproject.toml).
tradingagents_home = "/Users/joshua/TradingAgents"
