streamlit
pandas
python-dotenv
# The Agent Lab page also needs the local `tradingagents` package importable.
# Easiest: run this app with TradingAgents' own virtualenv (which already has
# streamlit + tradingagents). See README "Running".
